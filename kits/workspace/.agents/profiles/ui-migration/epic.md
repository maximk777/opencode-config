# <Epic title>

## Goal
What the stream achieves and why it is needed now.

## Scope
What the stream covers, with `screen:` keys once the map exists.

## Success criteria
How a person checks that the goal is reached.

## Out of scope
What the stream deliberately does not do.

## Open questions
Questions to answer before the stage closes. If there are none, leave `None.` as written.
None.

## Target users
Who uses the result and in which role.

## Product
The product or system the stream changes, with `domain:` and `repo:` keys.
