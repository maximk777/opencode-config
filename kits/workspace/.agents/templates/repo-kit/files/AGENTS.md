# __SERVICE__

Entry point for agents working in the `__SERVICE__` repository. Replace this text with what the repository does and where to start.

## Read first
- `rules/`: rules for every change in this repository.
- `skills/`: step-by-step recipes; follow a skill file exactly, in order.
- `local/`: rules and skills that belong to this repository only; the kit never overwrites them.

## Replace
Rename `rules/example.md` and `skills/example/` to real names, or delete them.
