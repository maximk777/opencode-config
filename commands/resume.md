---
description: Resume a change from its files
agent: orchestrator
---
Run ~/.config/opencode/bin/change-state ~/specs/<project> $ARGUMENTS and continue the change from that state using the change-execute skill.
