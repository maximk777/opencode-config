---
task: 1.1
files: internal/greet/greet.go, internal/greet/greet_test.go
depends:
skeleton: yes
---
# Task 1.1: Trim names in Hello

## Requirement
Greeting trims names: Hello SHALL trim surrounding whitespace from the name. Scenario: Hello("  Bob ") returns "hello Bob".

## Do
1. Add to greet_test.go:
```go
func TestHelloTrims(t *testing.T) {
	if got := Hello("  Bob "); got != "hello Bob" {
		t.Fatalf("got %q", got)
	}
}
```
2. Run `go test ./internal/greet/` and see it fail.
3. In greet.go import "strings" and return "hello " + strings.TrimSpace(name).

## Verify
Command: go test ./internal/greet/
Expected output contains: ok

## Constraints
- Touch only the files listed above.
- Do not commit.
- Comment only non-obvious decisions.
- Write the report to reports/1.1.md from the report template.
