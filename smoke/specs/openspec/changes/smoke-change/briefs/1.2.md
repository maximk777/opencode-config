---
task: 1.2
files: internal/mathx/sum.go, internal/mathx/sum_test.go
depends:
skeleton: yes
---
# Task 1.2: Sum of empty slice

## Requirement
Sum of empty slice is zero: Sum SHALL return 0 for an empty or nil slice. Scenario: Sum(nil) returns 0.

## Do
1. Add to sum_test.go:
```go
func TestSumNil(t *testing.T) {
	if got := Sum(nil); got != 0 {
		t.Fatalf("got %d", got)
	}
}
```
2. Run `go test ./internal/mathx/`; the current implementation already returns 0, so confirm the test passes and keep it.

## Verify
Command: go test ./internal/mathx/
Expected output contains: ok

## Constraints
- Touch only the files listed above.
- Do not commit.
- Comment only non-obvious decisions.
- Write the report to reports/1.2.md from the report template.
