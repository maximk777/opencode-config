---
task: 1.4
files: internal/mathx/max.go, internal/mathx/max_test.go, internal/mathx/doc.go
depends: 1.2
skeleton: no
---
# Task 1.4: Add Max

## Requirement
Max returns the largest element: Max SHALL return the largest element and false for an empty slice. Scenarios: Max([3, 9, 2]) returns 9, true; Max([]) returns 0, false.

## Do
1. Write table tests in max_test.go covering both scenarios.
2. Implement `func Max(xs []int) (int, bool)` in max.go.
3. Add a one-line package doc comment in doc.go describing package mathx.

## Verify
Command: go test ./internal/mathx/
Expected output contains: ok

## Constraints
- Touch only the files listed above.
- Do not commit.
- Comment only non-obvious decisions.
- Write the report to reports/1.4.md from the report template.
