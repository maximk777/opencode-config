---
task: 1.5
files: internal/clampx/clamp.go, internal/clampx/clamp_test.go
depends:
skeleton: no
---
# Task 1.5: Clamp rejects an inverted range

## Requirement
Clamp bounds a value: Clamp SHALL return the value limited to the range from lo to hi and SHALL return ErrInvalidRange when lo is greater than hi. Scenarios: Clamp(5, 1, 9) returns 5, nil; Clamp(-3, 1, 9) returns 1, nil; Clamp(5, 9, 1) returns 0, ErrInvalidRange.

## Do
1. Create package clampx with `var ErrInvalidRange = errors.New("clampx: lo greater than hi")` and `func Clamp(v, lo, hi int) (int, error)`.
2. Cover every scenario in clamp_test.go with a table test.

## Verify
Command: go test ./internal/clampx/
Expected output contains: ok

## Constraints
- Touch only the files listed above.
- Do not commit.
- Comment only non-obvious decisions.
- Write the report to reports/1.5.md from the report template.
