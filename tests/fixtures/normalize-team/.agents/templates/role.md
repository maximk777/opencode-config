# Role: <role>

## Read first
- `AGENTS.md`

## Repositories
`init` clones every entry of `repos.json` whose `roles` contains `<role>`. Add or skip repositories for yourself in `.local/repos.json`.

## Useful skills
- `.agents/skills/work-record/SKILL.md`
